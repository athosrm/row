export default function ReferencesIndex() {
	//const data = useLoaderData<LoaderData>();

	return (
		<>
			<div>
				<p>Last references accessed</p>
				{/* <p>{data.randomJoke.content}</p>
			<Link to={data.randomJoke.id}>"{data.randomJoke.name}" Permalink</Link> */}
			</div>
		</>
	);
}
